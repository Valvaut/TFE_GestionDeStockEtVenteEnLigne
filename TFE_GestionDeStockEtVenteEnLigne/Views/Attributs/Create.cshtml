@model TFE_GestionDeStockEtVenteEnLigne.Models.Attribut

@{
    ViewData["Title"] = "Create";
}

<h2>Création</h2>

<form asp-action="Create">
    <div class="form-horizontal">
        <h4>Attribut</h4>
        <hr />
        <div asp-validation-summary="ModelOnly" class="text-danger"></div>
        <div class="form-group">
            <label asp-for="Nom" class="col-md-2 control-label"></label>
            <div class="col-md-10">
                <input asp-for="Nom" class="form-control" />
                <span asp-validation-for="Nom" class="text-danger"></span>
            </div>
            <label asp-for="Mesure" class="col-md-2 control-label"></label>
            <div class="col-md-10">
                <input asp-for="Mesure" class="form-control" />
                <span asp-validation-for="Mesure" class="text-danger"></span>
            </div>
        </div>
        <div class="form-group">
            <div class="col-md-offset-2 col-md-10">
                <input type="submit" value="Créer" class="btn btn-default" />
            </div>
        </div>
    </div>
</form>

<div>
    <a asp-action="Index">Retour à la liste</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
