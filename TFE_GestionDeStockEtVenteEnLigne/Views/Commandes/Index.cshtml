@model IEnumerable<TFE_GestionDeStockEtVenteEnLigne.Models.Commande>

@{
    ViewData["Title"] = "Index";
}

<h2>Index</h2>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
                <th>
                    @Html.DisplayNameFor(model => model.DateCommade)
                </th>
                <th>
                    @Html.DisplayNameFor(model => model.EnCours)
                </th>
                <th>
                    @Html.DisplayNameFor(model => model.Envoie)
                </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.DateCommade)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.EnCours)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Envoie)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.ID">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.ID">Details</a> @*|
                <a asp-action="Delete" asp-route-id="@item.ID">Delete</a>*@
            </td>
        </tr>
}
    </tbody>
</table>
